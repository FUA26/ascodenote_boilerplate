# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME="Next.js Boilerplate"

# Development
NODE_ENV=development

# Skip env validation (useful for Docker builds)
# SKIP_ENV_VALIDATION=true

# Server-side Environment Variables
# Add your server-side env vars here (not prefixed with NEXT_PUBLIC_)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# API_SECRET_KEY=your_secret_key_here

# Client-side Environment Variables
# Add your client-side env vars here (must be prefixed with NEXT_PUBLIC_)
# NEXT_PUBLIC_API_URL=https://api.example.com
